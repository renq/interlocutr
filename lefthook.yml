assert_lefthook_installed: true

pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.go"
      run: go fmt ./...
    lint:
      glob: "*.go"
      run: golangci-lint run
    swag:
      glob: "*/http/*.go"
      run: swag init --outputTypes=json,yaml .
    test:
      run: make test
      root: .

lints:
  parallel: true
  commands:
    fmt:
      run: go fmt ./...
      root: .
    lint:
      run: golangci-lint run
      root: .
    test:
      run: make test
      root: .
